#pragma once

#include "pch.h"
#include "Tower/framework.h"

#include "sqlite3/sqlite3.h"

namespace Tower
{
    class DBManager
    {
    public:
        DBManager(void);

        ~DBManager(void);

    private:

    };
}